name: source_convert
description: Command line utility for converting source code into ML input.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.0-0 <3.0.0'

dependencies:
  analyzer: ^4.3.1
  args: ^2.3.1
  file: ^6.1.4
  path: ^1.8.0
  platform: ^3.1.0
  process: ^4.2.4
  pub_semver: ^2.0.0

dev_dependencies:
  flutter_lints: ^2.0.0
